# Core ML Libraries
torch>=2.5.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
bitsandbytes>=0.43.0
trl>=0.8.0

# Unsloth (fast training)

# Data Processing
pandas>=2.0.0
jsonlines>=4.0.0
numpy>=1.24.0

# Utilities
wandb>=0.16.0
gradio>=4.0.0
tqdm>=4.65.0

# Testing
pytest>=7.4.0
# Tokenization (needed for GGUF conversion)
sentencepiece>=0.1.99
